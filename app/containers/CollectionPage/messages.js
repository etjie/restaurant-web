/*
 * CollectionPage Messages
 *
 * This contains all the text for the CollectionPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CollectionPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CollectionPage container!',
  },
});
