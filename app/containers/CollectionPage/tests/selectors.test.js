// import { selectCollectionPageDomain } from '../selectors';

describe('selectCollectionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
