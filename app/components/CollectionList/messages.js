/*
 * CollectionList Messages
 *
 * This contains all the text for the CollectionList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CollectionList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CollectionList component!',
  },
});
