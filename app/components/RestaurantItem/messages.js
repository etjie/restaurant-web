/*
 * RestaurantItem Messages
 *
 * This contains all the text for the RestaurantItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RestaurantItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RestaurantItem component!',
  },
});
